services:
  bonsai:
    build:
      context: .
      args:
        BASE_IMAGE: nvidia/cuda:12.4.1-runtime-ubuntu24.04
        LLAMA_BACKEND: cuda
    image: bonsai2-server:cuda
    restart: unless-stopped
    ports:
      - "11437:8080"
    environment:
      BONSAI_PACKING: "PQ2_0"     # PTQ1_0 is faster on Ada-gen cards / L4; PQ2_0 elsewhere
      BONSAI_NGL: "99"            # offload every layer to GPU
      BONSAI_CTX: "32768"
      BONSAI_HOST: "0.0.0.0"
      PORT: "8080"
      BONSAI_VISION: "0"
    volumes:
      - /hdd1/model-files/bonsai2s:/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 0
              capabilities: [gpu]

volumes:
  bonsai_models: